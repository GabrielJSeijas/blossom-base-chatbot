MONGODB_URI=
# Genera una clave local con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Copia el valor real en backend/.env, no en este archivo de ejemplo.
MESSAGE_ENC_KEY_V1=
PORT=
DEMO_PORT=
LLM_PROVIDER=
MISTRAL_API_KEY=
MISTRAL_MODEL=
SUPPRESS_STARTUP_LOGS=
